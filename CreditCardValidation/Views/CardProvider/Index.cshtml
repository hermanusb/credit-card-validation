
@model IEnumerable<CreditCardValidation.Models.CardProvider>

<p>
    @Html.ActionLink("Create New", "Create")
</p>

@Html.React("CardProviderList", new
{
    initialData = Model,
    url = Url.Action("List"),
    //submitUrl = Url.Action("AddComment"),
    pollInterval = 2000
})

<script src="@Url.Content("~/Scripts/CardProviderIndex.jsx")"></script>
@Html.ReactInitJavaScript()

