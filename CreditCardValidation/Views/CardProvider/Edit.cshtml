
@model CreditCardValidation.Models.CardProvider

@Html.AntiForgeryToken()

<h2>Edit card Provider Details</h2>

@Html.React("CardProviderContainer", new
{
    data = Model,
    url = Url.Action("Edit")
})

<script src="@Url.Content("~/Scripts/CardProviderEdit.jsx")"></script>
@Html.ReactInitJavaScript()

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

